# WerePires 1.15.9 — 2026-08-16

## Fixed

- Limited the phone home-screen unread total to direct conversations that actually include the player.
- Included unread group messages in the phone home-screen total using each member's saved read watermark.
- Enforced contact blocking for incoming private calls, matching the existing "message and call blocking" setting.
- Corrected direct-call status text so callers see the contact they are calling instead of their own character name.
- Corrected missed-call notices so recipients always see the caller's character name.

## Documentation

- Added `CHANGELOG-FROM-VAMPIRESMP-1.0.6.md` as the consolidated inventory for the entire fork, not only the cellphone work.
- Verified the supplied `VampireSMP-1.0.6.jar` resources against the repository's original decompiled baseline before compiling the inventory.
- Corrected the new-tome count from nine to eleven and documented Fading and Scrying alongside the other additions.

## Verification

- Clean Maven packaging completed successfully.
- Development artifact: `WerePires-1.15.9-TESTBUILD.jar`.
- The network protocol remains disabled in the development-only TESTBUILD artifact.
