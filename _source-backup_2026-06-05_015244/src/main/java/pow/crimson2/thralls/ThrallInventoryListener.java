package pow.crimson2.thralls;

import org.bukkit.event.Listener;
import pow.crimson2.VampireSMPPlugin;

/** Retained as a registered Listener stub; thrall offer logic is now handled by Dialog callbacks. */
public class ThrallInventoryListener implements Listener {
    public ThrallInventoryListener(VampireSMPPlugin plugin) {}
}
