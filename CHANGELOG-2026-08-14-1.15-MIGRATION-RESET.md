# WerePires 1.15 — Legacy Migration and Session Reset Checkpoint

Checkpoint created August 14, 2026 at 16:17 CDT.

## Legacy migration

- Added one-time discovery of CellPhone.sk data in `plugins/skript-yaml`.
- Added importers for `phone_contacts.yml`, `phone_messages.yml`, `phone_settings.yml`, `phone_gps.yml`, `phone_notes.yml`, `phone_social.yml`, `phone_groups.yml`, and `phone_games.yml`.
- Migrates UUID/name caches, contact flags, nicknames, DMs/read state, phone settings/colors, GPS metadata, notes, social handles/follows/posts, group membership/read watermarks/messages, chips, leaderboards, Wordle streaks, and PvP records.
- Canonicalizes the legacy sorted-UUID conversation key into the native conversation format.
- Moves legacy `grp_<id>` message threads into native group records.
- Writes the native JSON only after a successful import and retries after failures.
- Leaves all legacy YAML files untouched as a recovery source.

## Session reset

- Corrected `/phonereset confirm` to preserve player identity records, DND, silent, vibrate, no-refresh, phone colors, and social handles.
- Reset now clears contacts, messages, GPS, notes, follows, posts/reactions, groups, games, records, and active matches.
- Reset now restores all session ID counters and notifies online players.
- Added the original `/cpreset` alias.

## Validation

- Maven compilation passed against Paper 1.21.10's Bukkit YAML API on Java 21.
- Final checkpoint uses a clean Maven package build.
- The network protocol kill switch remains disabled in the TESTBUILD artifact.
